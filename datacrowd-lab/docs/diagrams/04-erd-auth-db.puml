@startuml
title Auth DB (auth_db) — ERD (v1)

hide circle
skinparam linetype ortho

entity "users" as users {
  * id : BIGINT <<PK>>
  --
  username : VARCHAR(64) <<UNIQUE>>
  email : VARCHAR(255) <<UNIQUE>>
  password : VARCHAR(255)
  role : VARCHAR(32)
  created_at : TIMESTAMP
}

@enduml
